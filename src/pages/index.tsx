import type { NextPage } from "next";

import Scene from "./../studio/Scene";

const Home: NextPage = () => {
  return (
    <div style={{ height: "100vh" }}>
      <Scene />
    </div>
  );
};

export default Home;
